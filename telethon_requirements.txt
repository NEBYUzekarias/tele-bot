# telethon
telethon

# telethon will work a lot faster, since encryption and decryption will be made in C instead of Python
cryptg

# large images will be automatically resized when sending photos to prevent Telegram from failing with “invalid image”
Pillow

# telthon will be able to download WebDocument media files (otherwise you will get an error).
aiohttp

# will be used to extract metadata from files when sending documents.
# Telegram uses this information to show the song’s performer, artist, title, duration,
# and for videos too (including size). Otherwise, they will default to empty values,
# and you can set the attributes manually.
hachoir

# optional, if you are going to use proxies to connect telegram
pysocks

